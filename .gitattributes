# Force LF line endings on files that must run in a Linux container.
*.sh        text eol=lf
Dockerfile  text eol=lf
*.Dockerfile text eol=lf

# Force CRLF on Windows-only scripts
*.bat       text eol=crlf
*.ps1       text eol=crlf

# Default (let git auto-detect for the rest)
* text=auto
